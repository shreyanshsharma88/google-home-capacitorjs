import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreyansh.googleclone',
  appName: 'google-home-clone',
  webDir: 'dist'
};

export default config;
